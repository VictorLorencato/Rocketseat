<!DOCTYPE html>
<html lang="en">
<%- include('../partials/head'); %>
<body class="container">
    <%- include('../partials/header', {pagina: 'Página inicial'}); %>
    <main>
        <div class="jumbotron">
            <h1>Node com EJS</h1>
            <p>Vamos aprender a trabalhar com arquivos EJS</p>
            <p><%= subtitle %></p>
        </div>
        <ul>
            <% qualitys.forEach(function (qua){ %>
                <li>
                    <strong><%= qua.title %></strong><%= qua.message %>
                </li>
            <%}) %>
        </ul>
    </main>
    <%- include('../partials/footer') %>
</body>
</html>